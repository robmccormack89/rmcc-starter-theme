<aside class="uk-width-1-3@m">
	{% if sidebar_right %}
		{{ sidebar_right }}
	{% else %}
		<h3 class="uk-text-bold widget-title">{{ __('Sidebar', 'starter-wp-theme') }}</h3>
		<div class="uk-tile uk-tile-small uk-tile-muted">
			{{ __('Add some widgets to the Sidebar Widget Area in Appearance > Widgets.', 'starter-wp-theme') }}
		</div>
	{% endif %}
</aside>